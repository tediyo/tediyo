name: Update README

on:
  schedule:
    - cron: "*/30 * * * *"   # every 30 minutes
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Allow writing to repository
    
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # Step 1: Update README with timestamp
      - name: Update README with current date
        run: |
          echo "" >> README.md
          echo "<!-- Last updated: $(date) -->" >> README.md

      # Step 2: Generate GitHub Metrics and commit automatically
      - name: Generate GitHub Metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }} # or METRICS_TOKEN for private repos
          user: tediyo
          template: classic
          base: repositories
          plugin_repositories: yes
          filename: github-metrics.svg
          output_action: commit   # THIS makes metrics commit itself
          config_display_title: "📊 My Repository Stats"
